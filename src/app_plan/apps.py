"""Application settings for app_plan."""

from django.apps import AppConfig


class AppPlanConfig(AppConfig):
    """Main app config."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "app_plan"
